Cloudflare Pages Deploy

1. Repo'ga push qiling:
   git add .
   git commit -m "AZURA Reborn D1 R2 upgrade"
   git push

2. Cloudflare Pages settings:
   Framework preset: None
   Build command: (empty)
   Build output directory: /

3. Bindings:
   D1 binding name: DB
   D1 database: azura_db

   R2 binding name: MEDIA
   R2 bucket: azura-media

4. Test URL:
   /api/health
   /api/init
   /api/db
   /api/chapters
   /api/views
